<?xml version="1.0"?>
<odoo>
    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Property Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>

    <record id="estate_property_offer_form" model="ir.ui.view">
        <field name="name">Offer</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <field name="price"/>
                    <field name="partner_id"/>
                    <field name="status"/>
                </group>
            </form>
        </field>
    </record>

    <record id="estate_property_offer_tree" model="ir.ui.view">
        <field name="name">Offers</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <tree string="Offers" editable="top" create="1" edit="1" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <button name="accept_offer" string="Accept" type="object" icon="fa-check" invisible="status in ('accepted', 'refused')"/>
                <button name="refuse_offer" string="Refuse" type="object" icon="fa-times" invisible="status in ('accepted', 'refused')"/>
                <field name="status" column_invisible="1"/>
            </tree>
        </field>
    </record>
</odoo>
